<div class="split-card-home">
  <div class="split-card-title">
    <h3> <%= split.name %> </h3>
    <p> <%= split.payment_group.name %> </p>
  </div>
 <!--  CALCULATE THE TOTAL -->
    <h5>$ USER-BALANCE</h5>
<!--     LINKS TO THE IMG SCAN -->
    <%= link_to new_split_bill_path(split), class: 'button' do %>
      <i class="fas fa-camera"></i>
    <% end %>
</div>
